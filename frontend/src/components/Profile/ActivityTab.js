//this file handles activites by the user includding answers, questions, tags, badges, bookmarks, and reputation
import React, { Component, useState } from "react";
import axios from "axios";

function ActivityTab(props) {
  return (
    <div>
      Inside Activity Tab
      <p>
        this page handles activites by the user includding answers, questions,
        tags, badges, bookmarks, and reputation
      </p>
    </div>
  );
}

export default ActivityTab;
